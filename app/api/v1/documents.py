from __future__ import annotations

from fastapi import APIRouter

router = APIRouter()

# TODO: implement in Task 6
# GET    /documents      → list documents (tenant-scoped)
# DELETE /documents/{id} → delete document + chunks + S3 object
