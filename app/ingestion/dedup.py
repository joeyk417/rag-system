from __future__ import annotations

# TODO: implement in Task 3
# SHA-256 hash of raw PDF bytes
# Checks documents.file_hash in the tenant schema BEFORE Docling extraction
# If hash exists: return existing document_id immediately (no re-ingest)
# Reference: reference_notebooks/01. PageRAG - Data Ingestion.ipynb (compute_file_hash)
